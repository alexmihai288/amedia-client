import React from 'react'

const UserProfile = ({searchedUser,searchedUserPosts}) => {
  console.log(searchedUser)
  console.log(searchedUserPosts)
  return (
    <div>
      user profile
    </div>
  )
}

export default UserProfile